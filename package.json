{
  "name": "layers",
  "version": "1.0.0",
  "description": "State Layers",
  "main": "index.js",
  "directories": {
    "test": "tests"
  },
  "scripts": {
    "test": "jest",
    "build:web": "NODE_ENV=production BUILD_ENV=web rollup -c",
    "build:web:dev": "NODE_ENV=development BUILD_ENV=web rollup -c",
    "build:node": "NODE_ENV=production BUILD_ENV=node rollup -c",
    "build:node:dev": "NODE_ENV=development BUILD_ENV=node rollup -c"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/ingararntzen/statelayers.git"
  },
  "keywords": ["state-based", "layers", "playback"],
  "author": "Ingar M. Arntzen",
  "license": "GPL-3.0",
  "bugs": {
    "url": "https://github.com/ingararntzen/statelayers/issues"
  },
  "homepage": "https://github.com/ingararntzen/statelayers#readme",
  "devDependencies": {
  }
}
